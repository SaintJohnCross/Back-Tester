execution:
  logging: minimal
  data_mode: production
  run_scale: full
  compute_mode: parallel
  rate_limit: aggressive
  validation: strict
  env_dependency: locked
  caching: local_disk
  refresh_policy: refresh_on_missing
  error_policy: best_effort